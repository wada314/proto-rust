---
description: The `Result` type in puroro project
globs: 
---
Mostly, the project in `puroro` project are defining the own `ErrorKind` type and the `Result` type in its root module.
Make sure to use THAT `Result`, not the standard library's `Result` unless you think you really need to.